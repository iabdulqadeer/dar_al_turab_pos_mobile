name: dar_al_turab_pos
description: "Point-of-sale and invoicing for Dar Al Turab Foodstuff Trading LLC — weight-based meat wholesale with thermal receipt printing."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^3.3.2
  dio: ^5.10.0
  go_router: ^17.3.0
  flutter_secure_storage: ^10.3.1
  google_fonts: ^8.2.0
  intl: ^0.20.3
  device_info_plus: ^13.2.0
  shared_preferences: ^2.5.5
  # Bluetooth Classic (SPP) to the PM400. Android only; iOS would need a BLE
  # transport, which PrinterTransport is structured for but does not yet have.
  print_bluetooth_thermal: ^1.2.1
  mobile_scanner: ^7.4.0
  permission_handler: ^12.0.3

# Removed as unused:
#   riverpod_annotation / freezed_annotation / json_annotation and their
#     generators — every model is hand-written, no .g.dart or .freezed.dart
#     files exist anywhere.
#   esc_pos_utils_plus — EscPosEncoder builds the byte stream directly, since
#     the receipt arrives from the server pre-laid-out.
#   flutter_blue_plus — a second Bluetooth stack alongside the one actually
#     used, costing binary size and permissions for nothing.
#   connectivity_plus — no offline queue to trigger.

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.7

# Removed as unused: build_runner, freezed, json_serializable (no code
# generation in this project) and mocktail (tests use real objects and hand
# written fakes).

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Brand assets. Flutter picks the @2x/@3x variants automatically from the
  # 2.0x/ and 3.0x/ subfolders, so only the @1x path is listed.
  assets:
    - assets/brand/logo.png
    - assets/brand/logo_mono_light.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# ---------------------------------------------------------------------------
# Brand asset generation. Regenerate after changing anything in assets/brand/:
#   dart run flutter_launcher_icons
#   dart run flutter_native_splash:create
# ---------------------------------------------------------------------------
flutter_launcher_icons:
  image_path: "assets/brand/icon.png"

  android: "ic_launcher"
  # Android masks the outer third of an adaptive icon, so the foreground is
  # pre-padded to sit inside the 66% safe zone.
  adaptive_icon_background: "assets/brand/icon_background.png"
  adaptive_icon_foreground: "assets/brand/icon_foreground.png"
  min_sdk_android: 21

  # iOS forbids alpha in app icons; icon_ios.png is flattened onto white.
  ios: true
  image_path_ios: "assets/brand/icon_ios.png"
  remove_alpha_ios: true

flutter_native_splash:
  # The emblem is drawn on white, so a white splash keeps it consistent with
  # the logo's native context. Dark mode uses the mono-white variant.
  color: "#FFFFFF"
  image: "assets/brand/splash.png"
  color_dark: "#212120"
  image_dark: "assets/brand/splash_dark.png"

  android_12:
    # Android 12+ crops the splash icon to a circle and ignores the image's own
    # padding, so it gets its own tighter asset budget.
    image: "assets/brand/splash.png"
    color: "#FFFFFF"
    image_dark: "assets/brand/splash_dark.png"
    color_dark: "#212120"

  android: true
  ios: true
  web: false
